export enum AddStatus {
  // eslint-disable-next-line
  Full,
  // eslint-disable-next-line
  Add,
  // eslint-disable-next-line
  Wait,
}

export enum ResponseStatus {
  // eslint-disable-next-line
  Failure,
  // eslint-disable-next-line
  Confirmation,
  // eslint-disable-next-line
  Accomplishment,
  // eslint-disable-next-line
  Neutral,
  // eslint-disable-next-line
  None,
}

export enum FlagState {
  PREFILLED = 'prefilled',
  RECHECK = 'recheck',
}
